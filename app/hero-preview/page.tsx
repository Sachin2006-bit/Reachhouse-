import { Hero } from '@/components/hero/Hero'

export const metadata = { title: 'Hero Preview — ReachHouse' }

export default function HeroPreviewPage() {
  return (
    <main>
      <Hero />
    </main>
  )
}
